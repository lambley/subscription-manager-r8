<nav class="bg-gray-800 p-4">
  <div class="container mx-auto flex justify-between items-center">
    <div class="flex items-center space-x-4">
      <%= link_to "Home", root_path, class: "text-white hover:text-gray-300" %>
      <%= link_to "Subscriptions", subscriptions_path, class: "text-white hover:text-gray-300" %>
    </div>
    <div class="flex items-center space-x-4">
      <% if authenticated? %>
        <%= button_to "Log out", session_path, method: :delete, class: "bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600" %>
      <% else %>
        <%= button_to "Log in", new_session_path, method: :get, class: "bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600" %>
      <% end %>
    </div>
  </div>
</nav>
