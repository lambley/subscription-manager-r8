<nav class="fixed left-0 top-0 z-20 mx-auto flex h-[88px] w-full items-center border-b-4 border-border dark:border-darkNavBorder bg-white px-5 m500:h-16">
  <div class="container mx-auto flex justify-between items-center">
    <div class="flex items-center space-x-4">
      <%= link_to "Home", root_path, class: "text-black-500 hover:text-gray-500" %>
      <%= link_to "Subscriptions", subscriptions_path, class: "text-black-500 hover:text-gray-500" %>
    </div>
    <div class="flex items-center space-x-4">
      <% if authenticated? %>
        <%= render_button(
          text: 'Log out',
          link: session_path,
          button_type: 'button_to',
          method: :delete,
          bg_class: 'bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600',
        ) %>
      <% else %>
        <%= render_button(
          text: 'Log in',
          link: new_session_path,
          button_type: 'button_to',
          method: :get
        ) %>
      <% end %>
    </div>
  </div>
</nav>
